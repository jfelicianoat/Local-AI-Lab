# Fase 14 — Agent/tool experiments

A1 y M1 se planifican solo con capacidades observadas de AI Broker, modelo exacto, fallback
prohibido, exclusión del aprendizaje y passthrough de tools. Las ejecuciones autónomas se
delegan a un contrato de Athena. Local AI Lab no contiene otro runtime autónomo.

No se ha llamado al Broker ni a Athena; la puerta requiere capacidades y ejecuciones reales.
