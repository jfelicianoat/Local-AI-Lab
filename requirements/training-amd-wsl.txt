# Install only after the AMD/WSL capability phase selects an officially supported ROCm wheel source.
# The application deliberately does not pin a speculative torch build for untested hardware.
transformers>=4.53,<5
peft>=0.16,<1
accelerate>=1.8,<2
safetensors>=0.5,<1
